# Kasi Flavour – ML Dependencies
# Install: pip install -r ml/requirements_ml.txt

# ── Core ML ───────────────────────────────────────────────────────────────────
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2

# ── Collaborative Filtering (Priority 1 & 2 models) ──────────────────────────
# scikit-surprise uses Cython — install separately if build tools available
scikit-surprise==1.1.3
# Windows users: conda install -c conda-forge scikit-surprise

# ── Serialisation ────────────────────────────────────────────────────────────
# pickle is stdlib — no extra install needed

# ── Database (shared with backend) ───────────────────────────────────────────
sqlalchemy==2.0.30
pymysql==1.1.0